# .github/workflows/build.yml
name: Build MyMauiBlazorApp for iOS

on:
  push:
    branches: [ ios-build ]

jobs:
  build:
    runs-on: macos-15

    steps:
      - name: Checkout repository
        uses: actions/checkout@v3
        
      - name: Setup .NET
        uses: actions/setup-dotnet@v3
        with:
          dotnet-version: '10.0.x'

      - name: Install MAUI workload
        run: dotnet workload install maui

      - name: Restore NuGet packages
        run: dotnet restore ./MyMauiBlazorApp/MyMauiBlazorApp.sln

      - name: Publish MAUI iOS App
        run: dotnet publish ./MyMauiBlazorApp/MyMauiBlazorApp.csproj -c Release -f net10.0-ios /p:RuntimeIdentifier=ios-arm64